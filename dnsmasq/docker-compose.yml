version: '3.8'

services:
  dnsmasq:
    image: my-dnsmasq
    container_name: dnsmasq
    network_mode: host
    restart: always
    extra_hosts:
      - "example.com:192.168.1.100"
      - "anotherdomain.com:192.168.1.101"
